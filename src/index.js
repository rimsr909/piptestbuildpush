const app = require("./app");

const PORT = 3000;

app.listen(PORT, () => {
  console.log(`Server running on port ${PORT}`);
});


//haida el serveur 
// c'est le fichier d'entrre ( le fichier d'entree c'est celui qui demare les serveur)